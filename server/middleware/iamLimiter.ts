// TODO: Not working
import { RateLimiter } from "limiter";
import { JSONResponse } from "~~/iam/misc/types";

export default defineEventHandler(async (event) => {
  console.log("How to rate limit api?");
});
